{"patch":"*** Begin Patch\n*** Update File: shared/routes.ts\n@@\n   items: {\n@@\n     updateStatus: {\n       method: 'PATCH' as const,\n       path: '/api/items/:id/status',\n       input: z.object({\n-        status: z.enum(['reported', 'retrieved', 'donated', 'claimed']),\n+        // Support new workflow statuses; backend enforces valid values in practice\n+        status: z.string(),\n         claimedBy: z.string().optional(),\n       }),\n@@\n     },\n+    claim: {\n+      method: 'POST' as const,\n+      path: '/api/items/:id/claim',\n+      input: z.object({\n+        claimantName: z.string().optional(),\n+        claimantEmail: z.string().optional(),\n+        answers: z.array(z.object({ q: z.string(), a: z.string() })).optional(),\n+      }).optional(),\n+      responses: { 201: z.object({ claimId: z.number(), matchScore: z.number(), message: z.string() }) }\n+    },\n*** End Patch"}
